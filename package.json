{
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs --port ${PORT:-3000}",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --port ${PORT:-3000}"
  }
}